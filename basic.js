alert(); console.log()
// str, int, bool, null, undefined data type
const a = 'no update'
let b = 'yes update'
const c = 1
const d = true
const e = null
const f;
// array, object 만들기, 접근하기, 편집하기, 추가하기
const arr = []
arr[0]
arr[0] = 'new'
arr.push()
const obj = {
    name: 'joon',
    height: 183,
    funcName: function() {
        expression
    },
}
obj.name
obj.funcName()
obj.height = 184
obj.age = 34
// 함수 만들기
function myName(argument) {
    expression
    return something
    expression here never works
}
// 조건문
if (condition) {
    expression
} else if () {
    expression
} else () {
    expression
}
// and && or ||